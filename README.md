1.What is JSX, and why is it used in React?
=> JSX is syntax entension for Javascript XML.It lets us write HTML like markup inside Javascript / TypeScript.

2.What is the difference between props and state?
=> There are lot of difference between props and state but the most common differences are props is parent componant where as state the componant itself.

3.What does the useState hook do, and where did you use it in this project?
=> useState is a special Hooks for DeepDive. In this project i used in ExploreTech Componant.

4.What does the useEffect hook do, and why did you need it to load the JSON data?
=> useEffect is a another special Hooks for Brief. you want React to load/process that data after the component has rendered.I want React to load/process that data after the component has rendered.

5.Why does every item in a .map() list need a unique key prop?
=> A key gives react stable identity for each item, so it knows extactly which one moved, changed or was removed.

6.What is conditional rendering? Show one place you used it (example: the empty stack message). =>Conditional rendering means showing different UI depending on a condition.

7.How do you pass data from a parent component to a child component, and how does a child send something back to the parent?
=> In React, I pass data from a parent to a child through props. If a child needs to send data back to the parent, I pass a callback function from the parent as a prop, and the child calls that function with the required data.
